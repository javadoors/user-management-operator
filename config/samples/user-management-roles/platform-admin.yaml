apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: platform-admin
  labels:
    role-type: platform-role
    app.kubernetes.io/managed-by: user-management
  annotations:
    description: "提供在平台中的全部权限."
    author: "Platform Installer"
rules:
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["*"]